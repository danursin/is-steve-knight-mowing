import { Grid } from "semantic-ui-react";
import Layout from "../components/Layout";

const Home: React.FC = () => {
    return (
        <Layout>
            <p>I am in a layout!</p>
        </Layout>
    );
};

export default Home;
